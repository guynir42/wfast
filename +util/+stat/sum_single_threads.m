% Usage: S = sum_singles(matrix, varargin)
% Takes inputs of types double, single and uint16 and returns a sum along
% the 3rd dimension in single precision.
% 
% OPTIONAL ARGUMENTS:
%   -threads: split the work into multiple threads (default: 1, no threading).
% 
